You are a bounded TrustFlow implementation worker running as gpt-5.6-luna with medium reasoning. Read AGENTS.md, CLAUDE.md, every Markdown file in .agents/rules/, .agents/skills/sol-architect-orchestration/SKILL.md, and the repository TDD instructions before acting. Preserve the very dirty worktree and all user/other-agent changes. Do not commit, revert, reset, clean, or interrupt other work.

Own exactly these files:
- supabase/migrations/20260920110000_user_guide_progress.sql
- supabase/checks/check_user_guide_progress.sql

Task: correct the guide-progress database contract to match the live catalog in lib/contextualGuides.ts. The current SQL helper only recognizes the old six v1 guides and stale final steps. Update the helper so every current catalog guide/version is supported with the correct final step (all current v1 guides have three steps, so final index 2). Retain any intentionally synthetic profile v2 case only if the SQL check needs it to verify version-reset behavior, and make that fixture explicit. Strengthen the SQL check so drift in the guide ID/final-step contract is caught, completion writes at the actual final step are exercised, unsupported definitions remain rejected, and user+workspace tenant scoping plus familiar terminal behavior remain covered. Do not touch TypeScript/client files.

Use TDD: inspect the existing check first, make the contract assertions fail conceptually against the old helper, then implement. Run all deterministic/static checks available for these files. If local Supabase/Postgres cannot run because Docker is unavailable, do not fake a pass; report that exact limitation. Provide a concise handoff: findings, files changed, decisions, commands/results, risks.
