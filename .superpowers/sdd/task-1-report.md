Updated the reporting authorization check to support both installed signature families, verify the selected public and private ACLs, assert the authorized comparison returns its member, and check the documented `42501` foreign-target denial. Updated the report to reflect those changes.

`git diff --check` passed. Docker is unavailable because its named pipe is missing, and no local PostgreSQL client or SQL parser was available; the SQL check was not executed after editing. Nothing was staged or committed.