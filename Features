
2. Notifications
5.benchmarks
6. Security
More smart timer testing
when user plugs in a time because there's no time sessions, it should send to the manager of this task to approve it and it should have a notification to notify the manager.
get the reports system to track all the new features, such as the new analytics engine, the worker graphs, the personal comparison, etc.
Remove goal tracking from analytics in intelligence.
Restoring a project that has set tasks ends up with an error.
IMPORTANT

The pipeline_stage_targets table now tracks completed_at timestamps for accurate velocity tracking. Ensure your backend triggers are updated if any server-side reporting relies on this field.
Very good!
Everything is working finally perfectly, my only complains are minor. but for some reason, theme on mobile is broken and isnt the same as web/desktop, and ALOT of icons/texts are using outdated RGB stuff which is causing them to be colored Black.


one more thing, pages load without any kind of loading which makes them look very weird.
graphs misbehave on mobile and overflow inside their containers in a weird non dynamic way.
alot of components look great but arent built for mobile
project creation/editing should use the premium calendar modal. 
make sure that the calendar modal is working nicely on mobile.
profile doesnt conform to theme on mobile and stays black despite using white components.
screens are way too stuck at the top and need padding at the top on mobile.
i get an error trying to start the timer via the card view rather than [id].tsx
timerisland is broken interms of CSS on mobile.
most actions require loading states on web AND mobile.
most date pickers do NOT utilize the calendar on mobile.
Mobile QOL features on each screen.
