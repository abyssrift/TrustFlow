
2. Notifications
5.benchmarks
6. Security
More smart timer testing
when user plugs in a time because there's no time sessions, it should send to the manager of this task to approve it and it should have a notification to notify the manager.
Refactor targets page.
get the reports system to track all the new features, such as the new analytics engine, the worker graphs, the personal comparison, etc.
Remove goal tracking from analytics in intelligence.
Restoring a project that has set tasks ends up with an error.
IMPORTANT

The pipeline_stage_targets table now tracks completed_at timestamps for accurate velocity tracking. Ensure your backend triggers are updated if any server-side reporting relies on this field.